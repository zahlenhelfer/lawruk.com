using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;

namespace lawrukmvc.Models
{
    //public class CalendarRepository
    //{
    //    private lawrukEntities entities = new lawrukEntities();

    //    public List<ICalendarEntry> GetCalendarEntries()
    //    {
    //        using (lawrukEntities entities = new lawrukEntities())
    //        {
    //            //List<Person> people = entities.People.Where(p => p.BirthdayDay != null && p.BirthdayMonth != null).ToList();
    //            //List<Event> events = entities.Events.Where(e => e.Date >= DateTime.Now).ToList();
    //            //List<ICalendarEntry> peopleItems = people.Cast<ICalendarEntry>().ToList();
    //            //List<ICalendarEntry> eventItems = events.Cast<ICalendarEntry>().ToList();
    //            //List<ICalendarEntry> items = peopleItems.Union(eventItems).ToList();
    //            //return items.OrderBy(i => i.Date).ToList();
    //        }
    //        return null;
    //    }
    //}
}